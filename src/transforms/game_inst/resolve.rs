struct ResolveGameInst;

use super::super::GameInstanceTransform;

/*
impl GameInstanceTransform for ResolveGameInst {
    type Err;

    type Aux;

    fn transform_game_instance(
        &self,
        instance: &crate::proof::GameInstance,
    ) -> Result<(crate::proof::GameInstance, Self::Aux), Self::Err> {
        let types = instance.types();
        let consts = instance.as_consts();
        let game = instance.as_game();


    }
}
*/
