WHITESPACE = _{ " " | "\r" | "\n" | "\t" }

model = { "(" ~ modelline*~ ")" }
modelline = {"(define-fun" ~ name ~ "()" ~ ty ~ value ~ ")"}

name = @{ ( ASCII_ALPHA | "_" | "-" ) ~ ( ASCII_ALPHA | ASCII_DIGIT | "_" | "-" )* }
ty = { "Int" }
value = { ASCII_DIGIT+ }
