package Nonces {
    params {
        n: Integer,
        b: Bool,
    }
    
    oracle Sample() -> Bits(256) {
        ni         <-$ Bits(256);
        if b
        {assert false;}
        return ni;
    }
}
