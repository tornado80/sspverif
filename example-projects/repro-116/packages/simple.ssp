package Simple {
    params {
        b: Bool,
    }

    oracle Sample() -> Bits(256){
        if b {
            x <-$ Bits(256);
            return x;
        } else {
            x <-$ Bits(256);
            return x;
        }
    }
}
