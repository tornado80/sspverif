composition Monprfreal {
    const n: Integer;
    const prf: fn Bits(n), Bits(n) -> Bits(n);
    
    instance prf = PRFReal {
        params {
            n:   n,
            prf: prf,
        }
    }

    compose {
        adversary: {
            Eval: red,
            Get:  red,
        },
    }
}
