package MapGetOutputKey {
    import oracles {
        GETMAP(h: Bits(*)) -> Maybe(Bits(*)),
        GET(h: Bits(*)) -> Bits(*),
    }

    oracle GET(h: Bits(*)) -> Bits(*) {
        m <- invoke GETMAP(h);
        assert (m != None as Bits(*));
        k <- invoke GET(Unwrap(m));
        return k;
    }
}