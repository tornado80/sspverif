package GetOutputKey_Name_Level {
    import oracles {
        GET(h: Bits(*)) -> Bits(*),
    }

    oracle GET_Name_Level(h: Bits(*)) -> Bits(*) {
        k <- invoke GET(h);
        return k;
    }
}