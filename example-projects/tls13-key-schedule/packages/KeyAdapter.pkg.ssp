package KeyAdapter {
    import oracles {
        GET(h: Bits(*)) -> (Bits(*), Bool),
    }

    oracle GET1(h: Bits(*)) -> (Bits(*), Bool) {
        t <- invoke GET(h);
        return t;
    }

    oracle GET2(h: Bits(*)) -> (Bits(*), Bool) {
        t <- invoke GET(h);
        return t;
    }
}