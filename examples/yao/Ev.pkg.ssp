package Ev {
    params {
        n: Integer,
    }

    state {
    }

    oracle EVAL(j: Integer,l: Integer,r: Integer,op: fn (Bool,Bool) -> Bool) -> Bits(m) {
        K <- invoke GETKEYS();
        z <- invoke GETBIT();
        r <-$ Bits(n);
        if (b==z)
        {c <- encn(K[z],mzero,r);}
        else
        {c <- encn(K[z],mone,r);}
        return c;
    }

}